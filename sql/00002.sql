CREATE TABLE list (id BIGSERIAL PRIMARY KEY, account_id BIGINT NOT NULL REFERENCES account (id) ON DELETE CASCADE, name VARCHAR(250) NOT NULL)
CREATE TABLE list_item (id BIGSERIAL PRIMARY KEY, list_id BIGINT NOT NULL REFERENCES list (id) ON DELETE CASCADE, title VARCHAR(250) NOT NULL, notes TEXT, url TEXT)